mkdir ~/Site 1er verion
# Creates a directory for your project called "Hello-World" in your user directory

cd ~/Site 1er version
# Changes the current working directory to your newly created directory

git init
# Sets up the necessary Git files
